<%= form_with(model: komanda, local: true) do |form| %>
  <% if komanda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(komanda.errors.count, "error") %> prohibited this komanda from being saved:</h2>

      <ul>
      <% komanda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :komanda_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
